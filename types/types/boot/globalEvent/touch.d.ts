import { App } from "../../index";
export declare const globalTouchStart: (event: TouchEvent) => boolean;
export declare const globalTouchEnd: (event: TouchEvent, yDiff: number, time: number, app: App) => boolean;
